# WAV

WAV and voice generation are deferred.

No executable WAV implementation belongs here until an explicit task approves provider policy, voice dataset handling, tests, and a dependency boundary to `libraries/wav`.
