# SRT

SRT and timestamp JSON generation are deferred.

No executable caption implementation belongs here until an explicit task approves timestamp alignment, tests, and a dependency boundary to `libraries/srt`.
