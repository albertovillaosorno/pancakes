# Catalog Tests

Catalog tests cover canonical catalog models, compilation, drift checks, and
raw-spec ingestion contracts.

## File Boundary

This README owns the catalog test slice. Blueprint validation and MCP behavior
consume catalog outputs but do not define this slice.
