# Raw Spec Tests

Tests here protect Make.com raw-spec refresh, parsing, manifest, and live-adapter
guard behavior.

## File Boundary

This README owns raw-spec test coverage. It does not test catalog projection
semantics beyond the ingestion contract.
