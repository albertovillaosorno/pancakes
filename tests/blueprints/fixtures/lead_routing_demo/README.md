# Lead Routing Demo Fixtures

These fixtures define a sanitized portfolio demo for lead intake and routing.
They intentionally use fake names, fake email domains, and placeholder
Make Data Store destinations.

## File Boundary

This README owns the lead-routing demo fixture boundary. The files here are
portfolio-safe examples only and must not be treated as importable output until
the catalog-backed render gate passes.

The current scenario is draft-only. It must not be published as importable Make
blueprint output until catalog-backed validation and the render gate pass.
